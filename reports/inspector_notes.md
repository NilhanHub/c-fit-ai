# Inspector Notes

`app/inspector_cli.py` is a thin local inspector for Prompt #03 outputs.

Usage:

```bash
uv run python app/inspector_cli.py --scenario mixed_market_base --limit 10
```

This is intentionally a utility-first local view, not a polished dashboard.
