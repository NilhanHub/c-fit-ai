# Prompt #05 Feed Rebuild

The feed is now a pulse surface. It opens with a sticky command layer, then a delta stage, then the urgent story reel, then the ranked jobs stack. Support systems moved into rails and cards that explain why the user should return rather than acting as passive appendix blocks. The goal is to make movement tangible before the user scrolls far.
