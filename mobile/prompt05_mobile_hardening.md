# Prompt #05 Mobile Hardening

The mobile pass keeps the feed dominant, keeps top actions near the top of the thumb path, and moves reporting/composer flows into sheets. The main feed now appears before utility rails on small screens, which fixes a key weakness discovered during the rendered QA pass.
